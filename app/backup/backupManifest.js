export function createBackupManifest({ backupId, type='full', included={}, counts={}, files=[] }) { return { backupId, type, appName:'ai-workflow-runner', createdAt:new Date().toISOString(), appVersion:'0.1.0', included:{database:true,workflows:true,prompts:true,templates:true,variables:true,settings:true,outputs:false,exports:false,browserProfile:false,...included}, counts, files, checksum:null }; }
