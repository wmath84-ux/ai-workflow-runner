import React, { useState } from 'react';
export default function PackageImportBox({ onImport, onValidate }) { const [path,setPath]=useState(''); const [strategy,setStrategy]=useState('duplicate'); return <section className="card"><h2>Import Workflow Package</h2><input placeholder=".aiworkflowpkg path" value={path} onChange={e=>setPath(e.target.value)}/><select value={strategy} onChange={e=>setStrategy(e.target.value)}><option value="duplicate">Duplicate with suffix</option><option value="skip">Skip conflicts</option><option value="replace">Replace conflicts</option></select><div className="buttonRow"><button className="secondaryButton" onClick={()=>onValidate(path)}>Validate Package</button><button className="primaryAction" onClick={()=>onImport(path,{strategy})}>Import Package</button></div></section>; }
