import React, { useState } from 'react';
import ResultStepCard from './ResultStepCard.jsx';
export default function ResultOutputViewer({ run }) { const [tab,setTab]=useState('final'); const final=run?.checkpoint?.finalOutputPath; return <section className="card"><h2>Output Viewer</h2><div className="buttonRow"><button className="secondaryButton" onClick={()=>setTab('final')}>Final Output</button><button className="secondaryButton" onClick={()=>setTab('steps')}>Step Outputs</button><button className="secondaryButton" onClick={()=>setTab('raw')}>Raw JSON</button></div>{tab==='final' ? <pre>{final ? `Final output file: ${final}` : JSON.stringify(run?.checkpoint?.completedOutputs ?? {}, null, 2)}</pre> : null}{tab==='steps' ? <div className="grid">{(run?.steps??[]).map(step=><ResultStepCard key={step.id} step={step}/>)}</div> : null}{tab==='raw' ? <pre>{JSON.stringify(run,null,2)}</pre> : null}</section>; }
