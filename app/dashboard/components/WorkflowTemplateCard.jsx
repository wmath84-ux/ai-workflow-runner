import React from 'react';
export default function WorkflowTemplateCard({ template, onSelect, onDuplicate, onFavorite, onDelete }) { return <article className="card compact"><h3>{template.name} {template.isFavorite?'★':''}</h3><p>{template.description}</p><small>{template.category} · {(template.tools??[]).join(', ')} · {Object.keys(template.inputSchema??{}).length} inputs</small><div className="buttonRow"><button className="primaryAction" onClick={()=>onSelect(template)}>Use</button><button className="secondaryButton" onClick={()=>onDuplicate(template.id)}>Duplicate</button><button className="secondaryButton" onClick={()=>onFavorite(template.id)}>Favorite</button><button className="secondaryButton" onClick={()=>onDelete(template)}>Delete</button></div></article>; }
