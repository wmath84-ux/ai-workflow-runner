import React from 'react';
export default class ErrorBoundary extends React.Component { constructor(props){super(props); this.state={error:null};} static getDerivedStateFromError(error){return {error};} componentDidCatch(error){window.appAPI?.listLogs?.({level:'error'}).catch(()=>{}); console.error(error);} render(){if(this.state.error) return <div className="errorBox"><h2>Something went wrong</h2><p>{this.state.error.message}</p><button onClick={()=>window.location.reload()}>Reload app</button><button onClick={()=>this.setState({error:null})}>Go Home</button></div>; return this.props.children;} }
