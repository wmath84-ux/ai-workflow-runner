import React from 'react';
export default function PromptCard({ prompt, onEdit, onDuplicate, onDelete, onFavorite, onCopy }) { return <article className="card compact"><h3>{prompt.title} {prompt.isFavorite ? '★' : ''}</h3><p>{prompt.description}</p><small>{prompt.category} · {prompt.toolHint} · {(prompt.variables??[]).length} variables</small><p>{String(prompt.promptText).slice(0, 160)}...</p><div className="buttonRow"><button className="secondaryButton" onClick={()=>onEdit(prompt)}>Edit</button><button className="secondaryButton" onClick={()=>onDuplicate(prompt.id)}>Duplicate</button><button className="secondaryButton" onClick={()=>onFavorite(prompt.id)}>Favorite</button><button className="secondaryButton" onClick={()=>onCopy(prompt.promptText)}>Copy</button><button className="secondaryButton" onClick={()=>onDelete(prompt.id)}>Delete</button></div></article>; }
