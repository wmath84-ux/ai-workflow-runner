import React, { useState } from 'react';
export default function RestoreCard({ onRestore, onValidate }) { const [path,setPath]=useState(''); const [mode,setMode]=useState('database'); return <section className="card"><h2>Restore</h2><p>Restore can replace current app data. A pre-restore backup is created first.</p><input placeholder="Backup folder path" value={path} onChange={e=>setPath(e.target.value)}/><select value={mode} onChange={e=>setMode(e.target.value)}><option value="database">Database only</option><option value="workflows">Workflows only</option><option value="full">Full restore</option></select><div className="buttonRow"><button className="secondaryButton" onClick={()=>onValidate(path)}>Validate Backup</button><button className="primaryAction" onClick={()=>onRestore(path,{mode})}>Restore</button></div></section>; }
