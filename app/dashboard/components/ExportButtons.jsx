import React, { useState } from 'react';
export default function ExportButtons({ runId }) { const [message,setMessage]=useState(''); async function exp(fn){ const res=await fn(runId); setMessage(res.message || res.filePath); } return <div><div className="buttonRow"><button className="secondaryButton" onClick={()=>exp(window.appAPI.exportRunAsMarkdown)}>Export Markdown</button><button className="secondaryButton" onClick={()=>exp(window.appAPI.exportRunAsTxt)}>Export TXT</button><button className="secondaryButton" onClick={()=>exp(window.appAPI.exportRunAsJson)}>Export JSON</button><button className="secondaryButton" onClick={()=>exp(window.appAPI.exportRunAsZip)}>Export ZIP</button></div>{message ? <div className="emptyState">{message}</div> : null}</div>; }
