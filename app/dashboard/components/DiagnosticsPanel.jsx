import React, { useState } from 'react';
export default function DiagnosticsPanel({ onExport }) { const [logLimit,setLogLimit]=useState(500); const [includeOutputSummaries,setIncludeOutputSummaries]=useState(false); const [includeFullOutputs,setIncludeFullOutputs]=useState(false); return <section className="card"><h2>Diagnostics Export</h2><select value={logLimit} onChange={e=>setLogLimit(Number(e.target.value))}><option value={100}>100 logs</option><option value={500}>500 logs</option><option value={1000}>1000 logs</option></select><label><input type="checkbox" checked={includeOutputSummaries} onChange={e=>setIncludeOutputSummaries(e.target.checked)}/> Include output summaries</label><label><input type="checkbox" checked={includeFullOutputs} onChange={e=>setIncludeFullOutputs(e.target.checked)}/> Include full outputs (warning)</label><button className="primaryAction" onClick={()=>onExport({logLimit,includeOutputSummaries,includeFullOutputs})}>Export Diagnostics</button></section>; }
